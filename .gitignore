# Python
.venv/
__pycache__/
*.py[cod]
.Python

# Recordings (local POC output; keep examples/recordings/ as schema samples)
/recordings/

# OS / IDE
.DS_Store
.idea/
.vscode/

# GDevelop local exports / caches (if any)
gdevelop/**/Exported/
*.tmp

# Godot editor / import caches
godot/**/.godot/
.godot/

# Local desktop export output (T3.4)
/dist/

# Platform DB (local dev; production → Postgres via MW_PLATFORM_DB_URL)
/mw_platform/data/

# Private ops notes / deploy inject (host paths, branding, keys) — never commit
/docs/ops.local.md
/docs/*.local.md
/scripts/*.local.py
/scripts/*.local.sh

/*.zip
/kenney_*.zip
